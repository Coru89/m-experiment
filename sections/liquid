{%- for block in section.blocks -%}
  {%- case block.type -%}

    {%- when 'block_0' -%}
      {%- ########################################################################## 
      # Label: Content Position Mobile
      # Returns a string. i.e. the value of the selected option
      # Filters: https://shopify.dev/api/liquid/objects#string
      ########################################################################## -%}
      {{ block.settings.content_position_mobile }} 

      {%- ########################################################################## 
      # Label: Heading Font Opacity
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.heading_font_opacity }} 

      {%- ########################################################################## 
      # Label: Heading Drop Shadow
      # Returns: a boolean. i.e. A binary value, either true or false.
      ########################################################################## -%}
      {%- if block.settings.heading_drop_shadow -%}
        {%- # Write your logic here -%} 
      {%- endif -%}

      {%- ########################################################################## 
      # Label: Heading Drop Shadow X-Offset
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.heading_drop_shadow_x-offset }} 

      {%- ########################################################################## 
      # Label: Heading Drop Shadow Y-Offset
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.heading_drop_shadow_y-offset }} 

      {%- ########################################################################## 
      # Label: Heading Drop Shadow Color
      # Returns: a color object or blank(if no selection has been made)
      # Color Properties: https://shopify.dev/api/liquid/objects#color
      ########################################################################## -%}
      {{ block.settings.heading_drop_shadow_color }} 

      {%- ########################################################################## 
      # Label: Heading Font Size Desktop
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.heading_font_size_desktop }} 

      {%- ########################################################################## 
      # Label: Heading Font Size Mobile
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.heading_font_size_mobile }} 

      {%- ########################################################################## 
      # Label: Subheading Font Size Desktop
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.subheading_font_size_desktop }} 

      {%- ########################################################################## 
      # Label: Subheading Font Size Mobile
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.subheading_font_size_mobile }} 

      {%- ########################################################################## 
      # Label: Nudge Text X-Axis Desktop
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.nudge_text_x-axis_desktop }} 

      {%- ########################################################################## 
      # Label: Nudge Text X-Axis Mobile
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.nudge_text_x-axis_mobile }} 

      {%- ########################################################################## 
      # Label: Nudge Text Y-Axis Desktop
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.nudge_text_y-axis_desktop }} 

      {%- ########################################################################## 
      # Label: Nudge Text Y-Axis Mobile
      # Returns: a number or nil(if nothing has been entered)
      # Filters: https://shopify.dev/api/liquid/filters#math-filters 
      ########################################################################## -%}
      {{ block.settings.nudge_text_y-axis_mobile }} 

  {%- endcase -%}
{%- endfor -%}

{% schema %}
  {
  "name": "Code In Shopify Section",
  "settings": [],
  "blocks": [
    {
      "name": "Block 0",
      "type": "block_0",
      "settings": [
        {
          "type": "select",
          "id": "content_position_mobile",
          "label": "Content Position Mobile",
          "options": [
            {
              "value": "placeholder",
              "label": "placeholder"
            }
          ]
        },
        {
          "type": "range",
          "id": "heading_font_opacity",
          "label": "Heading Font Opacity",
          "min": 0,
          "max": 1,
          "default": 1
        },
        {
          "type": "checkbox",
          "id": "heading_drop_shadow",
          "label": "Heading Drop Shadow",
          "default": false
        },
        {
          "type": "range",
          "id": "heading_drop_shadow_x-offset",
          "label": "Heading Drop Shadow X-Offset",
          "min": -30,
          "max": 30,
          "step": 1,
          "default": 10,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "heading_drop_shadow_y-offset",
          "label": "Heading Drop Shadow Y-Offset",
          "min": -30,
          "max": 30,
          "step": 1,
          "default": 10,
          "unit": "px"
        },
        {
          "type": "color",
          "id": "heading_drop_shadow_color",
          "label": "Heading Drop Shadow Color",
          "default": "#FFFFFF"
        },
        {
          "type": "range",
          "id": "heading_font_size_desktop",
          "label": "Heading Font Size Desktop",
          "min": 20,
          "max": 180,
          "step": 1,
          "default": 36,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "heading_font_size_mobile",
          "label": "Heading Font Size Mobile",
          "max": 80,
          "step": 1,
          "default": 26
        },
        {
          "type": "range",
          "id": "subheading_font_size_desktop",
          "label": "Subheading Font Size Desktop",
          "max": 32,
          "step": 1,
          "default": 16
        },
        {
          "type": "range",
          "id": "subheading_font_size_mobile",
          "label": "Subheading Font Size Mobile",
          "min": 12,
          "max": 32,
          "step": 1,
          "default": 16,
          "unit": "px"
        },
        {
          "type": "range",
          "id": "nudge_text_x-axis_desktop",
          "label": "Nudge Text X-Axis Desktop",
          "min": -30,
          "max": 30,
          "step": 1,
          "default": 0,
          "unit": "%"
        },
        {
          "type": "range",
          "id": "nudge_text_x-axis_mobile",
          "label": "Nudge Text X-Axis Mobile",
          "min": -30,
          "max": 30,
          "step": 1,
          "default": 0,
          "unit": "%"
        },
        {
          "type": "range",
          "id": "nudge_text_y-axis_desktop",
          "label": "Nudge Text Y-Axis Desktop",
          "min": -30,
          "max": 30,
          "step": 1,
          "default": 0,
          "unit": "%"
        },
        {
          "type": "range",
          "id": "nudge_text_y-axis_mobile",
          "label": "Nudge Text Y-Axis Mobile",
          "min": -30,
          "max": 30,
          "step": 1,
          "default": 0,
          "unit": "%"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Code In Shopify Section"
    }
  ]
}  
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}