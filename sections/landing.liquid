
{{ section.settings.title }} 

{%- assign menu = section.settings.menu.links -%}
{%- for link in menu -%}
  {{ link.title | link_to: link.url }}
{%- endfor -%}

{% schema %}
  {
  "name": "Landing Page",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu"
    }
  ],
  "presets": [
    {
      "name": "Landing Page"
    }
  ]
}  
{% endschema %}